# Cross-language "Hello, World!" example

## Builld, run, and cleanup the docker image

```console
docker build -t docker-hello-interop . && docker run --rm docker-hello-interop & docker rmi docker-hello-interop
```
